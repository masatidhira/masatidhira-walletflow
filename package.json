{
  "name": "masatidhira-walletflow",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "author": "masatidhira",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
